# The desktop package consumes the root project as a local file dependency.
# Keep generated installers and the desktop wrapper out of that nested copy.
desktop/
release/
release-test/
release-*/
.git/
.codex/
.agents/
node_modules/
tests/
