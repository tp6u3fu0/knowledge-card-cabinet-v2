# 前端公開設定
NEXT_PUBLIC_DOWNLOAD_URL=

# 常駐主機版（npm run serve / Dockerfile.standalone）
# 資料與模型位置
KCC_DATA_DIR=
KCC_MODELS_DIR=

# 介面監聽位址。設成 0.0.0.0 會對整個網路開放，而介面本身沒有登入機制，
# 請只在私人網路（例如 Tailscale、WireGuard）中這樣做。
KCC_HOST=127.0.0.1
KCC_PORT=3000

# 固定 API 權杖。留空則每次啟動隨機產生。
KCC_API_TOKEN=
