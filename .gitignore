# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/desktop/node_modules
/.npm-cache/

# build output
/dist/
/dist-site/
/.vinext/
/.next/
/out/
release/
tsconfig.tsbuildinfo

# local data written by `npm run dev:api` and `npm run serve`
/data/

# env files (can opt-in for committing if needed)
.env*

# misc
.DS_Store
*.pem
/coverage

# embedding weights fetched by `npm run models:bundle` for the installer.
# build/icon.ico is committed; these are ~130 MB of downloadable files.
/build/models/

# debug
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
release-dir/

# 產品規格書：外部交付的文件，不是這個 repo 的產物。
# 內容已經吸收進 CLAUDE.md 第 1–2 節。
/retrieve-first-spec.rtf
